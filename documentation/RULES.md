# Rules

This document describes how the app ranks a time window.

The app starts with a neutral score.

It adds points for helpful windows.

It removes points for difficult windows.

It adds context from the selected activity.

It returns a score, a band, reasons, and cautions.
